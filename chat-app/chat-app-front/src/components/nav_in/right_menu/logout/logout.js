import React from 'react'
import classes from "./logout.module.css"

const logout = (props) => {
    return (
        <button className={classes.logout} type="button" >Logout</button>
    )
}

export default logout
